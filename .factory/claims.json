[
  {
    "id": "demo-isolation",
    "claim": "Demo changes never touch real workspace data.",
    "where": "demo banner, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser; seed a real sentinel, enter /demo, change and reset sample data, leave for real, and assert the real sentinel is unchanged"
  },
  {
    "id": "boundary-check",
    "claim": "The preflight blocks another client name or a configured redaction term.",
    "where": "landing preview, demo result, README",
    "test": "npm run test:e2e -- --grep @claim:boundary-check",
    "sandbox": "fresh /demo state; use Northstar Coffee, enter Juniper Legal and a configured term, and assert the matching checks block the session"
  },
  {
    "id": "provenance-export",
    "claim": "The demo exports a clearly marked sample record with each checked source.",
    "where": "how it works, demo delivery ledger, README",
    "test": "npm run test:e2e -- --grep @claim:provenance-export",
    "sandbox": "fresh /demo state; complete a clean sample check, download JSON, and assert sample status, every selected source, checks, and no real launch receipt"
  },
  {
    "id": "device-local",
    "claim": "The browser workspace does not send workspace data off-origin.",
    "where": "landing facts, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:device-local",
    "sandbox": "fresh browser context; record every request while creating and checking a workspace and assert workspace data never leaves the product origin"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts, demo offline state, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load /demo, wait for the service worker, go offline, reload, and assert the seeded session remains usable"
  },
  {
    "id": "hosting-routes",
    "claim": "Every public route opens directly with security headers, and unknown routes return the designed 404.",
    "where": "site behavior and handoff",
    "test": "npm run test:e2e -- --grep @claim:hosting-routes",
    "sandbox": "built preview; open every M1 route directly, inspect required headers, and assert an unknown path returns the designed 404 with a home action"
  }
]
