# Components

Place semantic, storage-agnostic UI primitives here. The authoritative
inventory and state/accessibility contracts are in `.factory/components.md`.
Feature orchestration does not belong in this directory.
