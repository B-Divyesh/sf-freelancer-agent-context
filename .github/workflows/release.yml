name: Release desktop apps
on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      manifest_only:
        description: Rebuild checksums for the current package version
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    if: github.event_name != 'workflow_dispatch' || inputs.manifest_only != true
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: --target aarch64-apple-darwin
            rust_target: aarch64-apple-darwin
          - platform: macos-latest
            args: --target x86_64-apple-darwin
            rust_target: x86_64-apple-darwin
          - platform: ubuntu-22.04
            args: ''
            rust_target: ''
          - platform: windows-latest
            args: ''
            rust_target: ''
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - uses: dtolnay/rust-toolchain@stable
        with: { targets: '${{ matrix.rust_target }}' }
      - name: Install Linux packages
        if: matrix.platform == 'ubuntu-22.04'
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libsecret-1-dev libfuse2 file rpm
      - run: npm ci
      - uses: tauri-apps/tauri-action@v0
        env: { GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
        with:
          tagName: v__VERSION__
          releaseName: Client Context Firewall v__VERSION__
          releaseBody: Unsigned desktop builds. Check SHA256SUMS before opening an installer.
          releaseDraft: false
          prerelease: false
          args: '${{ matrix.args }}'

  checksums:
    needs: build
    if: always() && (needs.build.result == 'success' || inputs.manifest_only == true)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download release assets
        env: { GH_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
        run: |
          release_tag="v$(node -p 'require("./package.json").version')"
          gh release download "$release_tag" --repo "$GITHUB_REPOSITORY" --dir release
      - name: Write checksums and manifest
        run: |
          cd release
          sha256sum * > SHA256SUMS
          export VERSION="$(node -p 'require("../package.json").version')"
          python3 - <<'PY'
          import json, os
          repo=os.environ['GITHUB_REPOSITORY']; version='v'+os.environ['VERSION']
          files=[f for f in os.listdir('.') if f not in ('SHA256SUMS','latest.json')]
          platforms={
            'mac': [f for f in files if f.endswith('.dmg')],
            'windows': [f for f in files if f.endswith(('.msi','.exe'))],
            'linux': [f for f in files if f.endswith(('.AppImage','.deb'))]
          }
          out={'version':version,'platforms':{k:[f'https://github.com/{repo}/releases/download/{version}/{n}' for n in v] for k,v in platforms.items()}}
          open('latest.json','w').write(json.dumps(out,indent=2))
          PY
      - name: Upload manifests
        env: { GH_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
        run: |
          release_tag="v$(node -p 'require("./package.json").version')"
          gh release upload "$release_tag" release/SHA256SUMS release/latest.json --repo "$GITHUB_REPOSITORY" --clobber
